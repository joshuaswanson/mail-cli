[project]
name = "mail-cli"
version = "0.1.0"
description = "CLI for Apple Mail"
requires-python = ">=3.11"
dependencies = ["click>=8.1"]

[project.scripts]
mail = "mail_cli.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
